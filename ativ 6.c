#include<stdio.h>
int main(){
    int i=0;//declarar que variavel comeca em 0
    do{
        printf("i: %d\n", i);
        i +=10;//pular de um em um ate 100
    }while(i <= 100 );//repetir
return 0;//finalização
}
